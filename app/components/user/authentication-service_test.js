'use strict';

describe('myApp.user.authentication-service module', function() {
  beforeEach(module('myApp.user.authentication-service'));

  describe('authentication service', function() {
    it('should ...', inject(function(version) {
      expect(version).toEqual('0.1');
    }));
  });
});
